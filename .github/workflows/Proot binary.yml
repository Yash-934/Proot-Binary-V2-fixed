name: Build PRoot Android ARM64 (Termux Style Clean)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      API: 21

    steps:

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y git wget unzip build-essential

    - name: Clone official PRoot
      run: |
        git clone https://github.com/proot-me/proot.git
        cd proot
        git checkout v5.4.0

    - name: Download Android NDK r26d
      run: |
        wget -q https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
        unzip -q android-ndk-r26d-linux.zip
        echo "NDK=$PWD/android-ndk-r26d" >> $GITHUB_ENV

    - name: Setup Android toolchain
      run: |
        echo "TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64" >> $GITHUB_ENV
        echo "TARGET=aarch64-linux-android" >> $GITHUB_ENV

    - name: Force disable talloc + pkg-config
      run: |
        cd proot/src
        sed -i 's/pkg-config/true/g' GNUmakefile || true

    - name: Build for Android ARM64 (PIE)
      run: |
        export CC=$TOOLCHAIN/bin/${TARGET}${API}-clang
        export CXX=$TOOLCHAIN/bin/${TARGET}${API}-clang++
        export AR=$TOOLCHAIN/bin/llvm-ar
        export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
        export STRIP=$TOOLCHAIN/bin/llvm-strip

        export PKG_CONFIG=/bin/false
        export CFLAGS="-fPIE -fPIC"
        export LDFLAGS="-fPIE -pie"
        export CPPFLAGS="-DHAVE_TALLOC=0"

        cd proot/src

        make WITHOUT_TALLOC=1 HAVE_TALLOC=0 \
             CC="$CC" AR="$AR" RANLIB="$RANLIB" \
             CFLAGS="$CFLAGS" LDFLAGS="$LDFLAGS" \
             CPPFLAGS="$CPPFLAGS" -j$(nproc)

    - name: Verify binary
      run: |
        file proot/src/proot || true
        readelf -h proot/src/proot | grep Type || true

    - name: Strip binary
      run: |
        $TOOLCHAIN/bin/llvm-strip proot/src/proot || true

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: proot-android-arm64-v5.4.0
        path: proot/src/proot
